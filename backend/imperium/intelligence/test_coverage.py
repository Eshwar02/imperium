"""Test Coverage Analyzer (TDD §4). Maps existing tests → code, finds gaps.

TODO(team): discover test files/frameworks, map to modules, report coverage gaps
that the Test-Generation agent should fill (PRD §10 baseline pass).
"""
from __future__ import annotations


def analyze(repo_path: str) -> dict:
    """Return {frameworks, covered_modules, gaps}. TODO(team)."""
    raise NotImplementedError("test coverage — team task")
